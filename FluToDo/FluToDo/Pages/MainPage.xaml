<?xml version="1.0" encoding="utf-8" ?>
<local:BasePage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:FluToDo.Pages"
             xmlns:vm="clr-namespace:FluToDo.ViewModels;assembly=FluToDo.ViewModels"
             xmlns:customViews="clr-namespace:FluToDo.CustomViews"
             x:Class="FluToDo.Pages.MainPage"
             Title="{Binding Title}"
             >

    <ContentPage.Content>
        <ListView  IsRefreshing="{Binding IsRefreshing}" ItemsSource="{Binding ItemList}" IsPullToRefreshEnabled="True" RefreshCommand="{Binding SynchronizeCommand}" >

            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <customViews:ItemView></customViews:ItemView>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
            
        </ListView>

    </ContentPage.Content>
    
</local:BasePage>